# URL of the deployed backend API. During local development this defaults
# to http://localhost:4000 if unset, so this file only needs to be created
# for production deployment (e.g. on Vercel), pointing at your deployed
# backend URL — no trailing slash.
NEXT_PUBLIC_API_URL=https://your-backend.up.railway.app
